/*
https://onlinejudge.org/external/109/10943.pdf
*/
#include <stdio.h>
#include <stdlib.h>

int main() {
    long long precalculate[100][100] = {0};

    int K, N;
    while (scanf("%d%d", &N, &K) && N) {
    }
}